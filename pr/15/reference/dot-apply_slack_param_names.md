# Restore Slack API parameter names

Translate package parameter names to Slack API parameter names.

## Usage

``` r
.apply_slack_param_names(x)
```

## Arguments

- x:

  (`list` or `character`) A named object that might have parameters to
  rename.

## Value

`x` with parameters renamed.
